# Scope

This slice intentionally does not claim native Pipecat compatibility, production HTTP adapters, response generation, or durable transcript persistence. It establishes the execution invariants those integrations must build on.
